package my.test.bed.epam.interviewing;

public class GenericsTest {


    /*class PairOfShoes {
        E left;
        E right;

        public boolean isPair() {
            return
                    left != null && right != null
                            && left.getSize() == right.getSize()
                            && left.getColor().equals(right.getColor());
        }

    }

    interface Shoes {
        int getSize();
        String getColor();
    }

*/
    /*public static void main(String[] args) {
        List<? super Number> list = new ArrayList<>();


        list.iterator().forEachRemaining(o -> System.out.println(o));


        consume(produce());
    }

    private static List<Number> produce() {
        List<Double> result = new ArrayList<>();
        result.add(11.1);
        return result;
    }


    private static void consume(List<? super Number> list) {
        System.out.println(list.get(0));
    }*/
}
